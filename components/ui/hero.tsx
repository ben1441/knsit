import React from "react";

const Hero = () => {
  return (
    <div className="flex flex-col items-center justify-center gap-6 bg-cyan-100 w-screen h-screen">
      hero
    </div>
  );
};

export default Hero;
